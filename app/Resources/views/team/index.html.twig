{% extends 'base_back.html.twig' %}

{% block title %}{{ parent() }}Team{% endblock %}

{% block body %}
    <!-- header Mission -->
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body app-heading">
                    <img class="profile-img" src="http://placehold.it/150x150">
                    <div class="app-title">
                        <div class="title"><span class="highlight">Team</span></div>
                        <div class="description">Configure your team member</div>
                    </div>
                    <a href="{{ path('team_new') }}">
                        <button type="button" class="btn btn-success">Add a new Member</button>
                    </a>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-xs-12">
          <div class="card">
            <div class="card-header">
              List of team member
            </div>
            <div class="card-body no-padding">
              <table class="datatable table table-striped primary" cellspacing="0" width="100%">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                    {% for team in teams %}
                        <tr>
                            <td>{{ team.name }}</td>
                            <td>{{ team.status }}</td>
                            <td>
                                <ul>
                                    <li>
                                        <a href="{{ path('team_edit', { 'id': team.id }) }}"><button type="button" class="btn btn-warning">modifier</button></a>
                                    </li>
                                </ul>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
              </table>
            </div>
          </div>
        </div>
    </div>

{% endblock %}


