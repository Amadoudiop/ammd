{% extends 'base_back.html.twig' %}
{% block title %}{{ parent() }}New Slide{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body app-heading">
                    <img class="profile-img" src="http://placehold.it/150x150">
                    <div class="app-title">
                        <div class="title"><span class="highlight">Slide creation</span></div>
                        <div class="description">Add a new Slide</div>
                    </div>
                    <a href="{{ path('slide_index') }}">
                        <button type="button" class="btn btn-info">
                            <i class="fa fa-reply" aria-hidden="true"></i>
                        </button>
                    </a>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <div class="col-md-6">
                        <img id="preview_img" src="http://www.placehold.it/1920x1080" class="img-responsive">
                    </div>
                    <div class="col-md-6">
                        {{ form_start(form) }}
                            {{ form_widget(form) }}
                            <input type="submit" value="Create" class="btn btn-primary">
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>  
{% endblock %}
